<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block px-5 w-full outline-none rounded-md py-2 bg-zinc-800" type="text" placeholder="Title" name="title" >
                <textarea class="block w-full outline-none rounded-md px-5 mt-2 py-2 re bg-zinc-800" placeholder="Write your task" name="details"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="Create Task"> 
            </form>
        </div>
        <div class="tasks flex gap-3 p-10 flex-wrap">
            <% if(files.length > 0) { %>
                <% files.forEach(function(val){ %>
                    <div class="task w-72 px-3 min-w-72 py-4 bg-zinc-800 rounded-md">
                        <h1 class="text-white text-3xl tracking-tighter"> <%= val %> </h1>
                        <a class="text-blue-500 inline-block mt-3" href="/files/<%= val %>">Read More.</a>
                    </div>
                <% }) %>
            <% } else { %>  <!-- Corrected closing tag here -->
                <h3 class="text-zinc-600">No tasks yet...</h3>
            <% } %> <!-- Corrected closing tag here -->
        </div>
    </div>
</body>
</html>
